; Keep the lockfile's baked-in settings stable regardless of any user-level
; pnpm config (a global auto-install-peers=false regenerates the lockfile in
; a shape the pinned pnpm@9 on Vercel rejects with LOCKFILE_CONFIG_MISMATCH).
auto-install-peers=true
